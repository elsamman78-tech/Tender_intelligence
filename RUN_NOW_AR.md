# تشغيل Tender Intelligence الآن

## أسرع طريقة لمشاهدة النظام الحقيقي الآن

1. افتح صفحة الـRepository الرئيسية.
2. اضغط زر **Deploy to Render** الموجود أعلى README.
3. سجّل الدخول إلى Render باستخدام GitHub.
4. راجع أن الخطة هي **Free** ثم اضغط **Apply / Deploy**.
5. انتظر اكتمال Build وDeploy.
6. افتح رابط `onrender.com` الذي يظهر أعلى صفحة الخدمة.

المسارات المهمة بعد التشغيل:

- `/` لوحة النظام الرئيسية
- `/discovery` شاشة Discovery
- `/sources` المصادر
- `/api/v1/health` فحص الصحة

## مهم

هذه نسخة اختبار سريعة على Render Free:

- لا تحتاج بطاقة للديمو المجاني حسب وثائق Render الحالية.
- الخدمة قد تتوقف بعد 15 دقيقة من عدم الاستخدام ثم تستيقظ عند أول زيارة.
- التخزين المحلي مؤقت، لذلك SQLite والملفات المرفوعة قد تضيع عند إعادة التشغيل أو إعادة النشر.
- لا تعتبر هذا نشرًا إنتاجيًا أو 24/7 دائمًا.

المسار النهائي الدائم يظل VM + Docker + تخزين دائم + طبقة وصول آمنة.
