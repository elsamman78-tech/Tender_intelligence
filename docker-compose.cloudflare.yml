services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: tender-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      tender-intelligence:
        condition: service_healthy
